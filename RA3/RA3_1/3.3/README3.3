Práctica 3.3.1: Hardening Avanzado de Servidor Apache

Este repositorio contiene una imagen de Apache2 configurada bajo estándares de seguridad profesional (Hardening). El objetivo es minimizar la superficie de ataque y prevenir la fuga de información sensible.
🛡️ Medidas de Seguridad Implementadas
1. Minimización de Información (Info Leakage)

    ServerTokens Prod: Se ha configurado el servidor para que solo responda "Apache" en las cabeceras, ocultando la versión exacta y el sistema operativo.

    ServerSignature Off: Eliminación de la línea de firma en páginas de error generadas por el servidor.

    FileETag None: Desactivación de ETags para evitar la exposición de números de inodo del sistema de archivos.

2. Control de Métodos HTTP

    TraceEnable Off: Bloqueo del método TRACE para mitigar ataques de Cross-Site Tracing (XST).

    LimitExcept: Restricción de métodos permitidos únicamente a GET, POST y HEAD. Cualquier otro método devuelve un error 405 Method Not Allowed.

3. Cabeceras de Seguridad (Security Headers)

    X-Frame-Options (SAMEORIGIN): Protección contra ataques de Clickjacking.

    X-XSS-Protection: Activación del filtro contra Cross-Site Scripting (XSS) en modo bloqueo.

    HttpOnly & Secure Flags: Configuración predeterminada para el manejo seguro de cookies de sesión.

4. Hardening de Directorios

    Options -Indexes: Desactivación del listado automático de directorios (Directory Browsing).

    Options -Includes: Desactivación de Server Side Includes (SSI) para prevenir inyección de scripts.

🚀 Despliegue del Contenedor

Para ejecutar este servidor protegido, utiliza el siguiente comando:
Bash

docker run -d \
  --name servidor_hardening \
  -p 80:80 \
  educiber/pps3.3.1:latest

🧪 Verificación de Hardening

Puedes validar la configuración ejecutando los siguientes comandos desde tu terminal:

Verificar ocultación de versión:
Bash

curl -I http://localhost
# Resultado esperado: Server: Apache (sin números de versión)

Verificar bloqueo de TRACE:
Bash

curl -v -X TRACE http://localhost
# Resultado esperado: HTTP/1.1 405 Method Not Allowed
